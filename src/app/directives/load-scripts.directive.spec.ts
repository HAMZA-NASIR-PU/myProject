import { LoadScriptsDirective } from './load-scripts.directive';

describe('LoadScriptsDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadScriptsDirective();
    expect(directive).toBeTruthy();
  });
});
